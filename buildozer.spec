[app]

title = Apex Motivasyon  # App adı burada, istediğin gibi değiştir (telefonda görünecek)

package.name = apexmoti
package.domain = org.apex

source.dir = .
source.include_exts = py,png,jpg,kv,atlas

requirements = python3,kivy,kivymd

orientation = portrait

fullscreen = 0

android.api = 33
android.minapi = 21

android.permissions = INTERNET
